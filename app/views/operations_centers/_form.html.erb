<%= form_for(@operations_center) do |f| %>
  <% if @operations_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operations_center.errors.count, "error") %> prohibited this operations_center from being saved:</h2>

      <ul>
      <% @operations_center.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :unit_id %><br />
    <%= f.select :unit_id, options_from_collection_for_select(Unit.all, :id, :full_address, @operations_center.unit_id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
