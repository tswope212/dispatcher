<%= form_for(@task_waiver) do |f| %>
  <% if @task_waiver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_waiver.errors.count, "error") %> prohibited this task_waiver from being saved:</h2>

      <ul>
      <% @task_waiver.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.select :task_id, options_from_collection_for_select(Task.alphabetical, :id, :name, @task_waiver.task_id) %>
  </div>
  <div class="field">
    <%= f.label :waiver_id %><br />
    <%= f.select :waiver_id, options_from_collection_for_select(Waiver.all, :id, :name, @task_waiver.waiver_id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
