<p id="feedback_instructions">
  To provide feedback to Disaster Dispatcher, please submit the following form.
</p>

<%= form_for @feedback do |f| %>
  <%= f.text_field :name, :placeholder => 'Name', :tabindex => 1 %>
  <%= f.text_field :email, :placeholder => 'Email', :tabindex => 2 %>
  <%= f.submit 'SUBMIT', :id => 'feedback_submit', :tabindex => 4 %>
  <br>
  <%= f.text_area :message, :placeholder => 'Comments', :tabindex => 3 %>
<% end %>

<p>
  To contact a volunteer team, please select from the list below.
</p>

<table>
  <tr>
    <th>Team</th>
    <th>Primary Contact</th>
    <th>Email</th>
    <th>Phone</th>
    <th>GroupMe</th>
  </tr>
  <% @teams.each do |team| %>
    <tr class="<%= cycle 'white', 'light-gray' %>">
      <td><%= team.name %></td>
      <td><%= team.person.andand.name %></td>
      <td><%= team.email %></td>
      <td><%= team.phone_number %></td>
      <td><%= team.group_me_number %></td>
    </tr>
  <% end %>
</table>
